name: Sync Repository Labels

on:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/sync-labels.yml'
      - 'wiki-framework'
  workflow_dispatch:
  schedule:
    # Run weekly on Sundays at 00:00 UTC
    - cron: '0 0 * * 0'

jobs:
  sync-labels:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive
          # PAT with read access to the private BenDol/GithubWiki submodule.
          # actions/checkout copies it into the submodule git config; the default
          # GITHUB_TOKEN is scoped to this repo alone and cannot read it.
          token: ${{ secrets.FRAMEWORK_REPO_TOKEN || secrets.WIKI_BOT_TOKEN }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install @octokit/rest

      - name: Sync labels
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPOSITORY: ${{ github.repository }}
        run: node scripts/syncLabels.js
