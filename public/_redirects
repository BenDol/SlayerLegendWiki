# CDN Redirect for game assets (jsDelivr proxy)
/images/content/*  https://cdn.jsdelivr.net/gh/BenDol/SlayerLegendCDN@main/game-assets/images/:splat  200

# Cloudflare Pages SPA routing fallback
# Serve index.html for all routes that don't match static files
# This allows React Router to handle client-side routing

/*    /index.html   200
