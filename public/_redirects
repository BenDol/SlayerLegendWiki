# Game assets: /images/content/* is handled by functions/images/content/[[path]].js,
# which answers with a cacheable 302 to the raw GitHub URL of the CDN repository
# (the app and the prerendered crawler HTML rewrite the same paths themselves, so
# the Function only sees the long tail: image crawlers, link previews, old links).
# In dev, Vite proxy handles this (see vite.config.js)

# Hash URL Migration (Old: /#/path → New: /path)
# NOTE: Hash fragments (#/) are NOT sent to the server, so server-side redirects are impossible.
# Client-side redirect is implemented in wiki-framework/src/main.jsx to handle old bookmarks.
