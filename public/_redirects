# Game assets load directly from CDN (jsDelivr)
# Images with /images/content/* paths are resolved client-side by CustomImage component
# See src/utils/gameContentRenderer.jsx and src/services/imageService.js

# Cloudflare Pages SPA routing fallback
# Serve index.html for all routes that don't match static files
# This allows React Router to handle client-side routing

/*    /index.html   200
